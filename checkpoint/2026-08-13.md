# Checkpoint — 2026-08-13

## Infra
- Postgres db dropped/recreated/restored from `db/backups/mk_paper_mill_20260812_1156.sql`
- `backend/node_modules`, `frontend/node_modules` were missing — installed
- Ports: frontend moved 9990→3333 (`start.bat`, `stop.bat`, `start.ps1`, `vite.config.js`)
- `head.store@mkpapermill.com` password reset to `Head@1234` (doc value didn't match live hash)
- `logs/` wired for live error capture: `backend-error.log`, `db-error.log` (split by
  SQLSTATE code), `frontend-error.log` (browser errors shipped via `errorLogger.js`)

## Audits + fixes (see `agents/` folder for full detail per session)
- Full-stack schema/backend/frontend/workflow sync audit — 16 real breaks fixed
- Hardcoded-data audit across all 35 frontend pages — 9 real violations fixed
- Store ERP 11-step workflow (vendor→item→PR→PO→GRN→issue) — verified + wired full DML
- Mechanical store sub-categorized into 17 real subcategories from
  `Projects_Requirement/MECHANICAL STORE AUGUST-2026.xlsx`, reusable importer wired
  into the app (sync button + dry-run preview), 26 missing items provisioned, 17 wrong
  item names corrected

## Migrations added this session
- `db/migration_indent_status_widen.sql`
- `db/migration_mechanical_subcategories.sql`
- `db/migration_mechanical_missing_items.sql`

## Open items — need a human call
4 item-code conflicts (db description vs excel description disagree) — full list in
root `checkpoint.json` → `openItems`. Not auto-resolved, risk of corrupting a correct
record either way.
